# Install the current package (coaching_mcp) in editable mode
-e .

# FastMCP for on-demand coaching tools - use latest for Horizon compatibility
fastmcp>=2.0.0

# Prefect for orchestration
prefect>=3.0.0

# Claude API for analysis
anthropic>=0.40.0

# Database drivers
psycopg2-binary>=2.9.0   # Neon Postgres (sync)
asyncpg>=0.29.0          # Async Postgres driver

# Data validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client
httpx>=0.27.0

# FastAPI for webhook endpoint
fastapi>=0.115.0
uvicorn>=0.30.0

# Report generation
jinja2>=3.1.0

# Environment variables
python-dotenv>=1.0.0

# Token counting for chunking
tiktoken>=0.7.0

# Vector search (optional)
pgvector>=0.3.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Code quality
black>=24.0.0
ruff>=0.3.0
mypy>=1.8.0
